#include <stdio.h>
/**
 * main - print 10 times the alphabet, in lowercase
 *
 * Return:Always 0
 */
void print_alphabet_x10(void);
int main(void)
{
	char(k);
	int (j);

	while (j <= 9)
	{
		for (k = 'a'; k <= 'z'; k++)
		{
			putchar (k);
		}
		putchar('\n');
	}
		j++;
	return (0);
}
