#include <stdio.h>
#include"main."
/**
 * Main - print_alphabet_x10- Prints alphabets 10 times
 *
 */
void print_alphabet_x10(void);
int main(void)
{
	char(k);
	int (j);

	while (j <= 9)
	{
		for (k = 'a'; k <= 'z'; k++)
		{
			putchar (k);
		}
		putchar('\n');
	}
		j++;
}
